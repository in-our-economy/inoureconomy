---
layout: post
title: "Announcing In Our Economy"
subtitle: "A 5-month experiment leading up to the release of the Survey of Consumer Finances"
date: 2026-04-01
categories: [Commentary]
tags: [inequality, wealth]
author: "Cyrus O'Brien"
featured: true
---


### A 5-month experiment leading up to the release of the Survey of Consumer Finances in the Fall

In October 2026 - just weeks before the midterm elections - the Federal Reserve should release the Survey of Consumer Finances (SCF). This survey is the gold standard source for data about economic inequality in the U.S. and the source of almost every stat you've ever heard about the racial wealth gap or about how much wealth the 1% or 0.1% have compared to the rest us.

When the data is released, experts expect the Fed's summary to highlight increases in wealth. Americans, it will say, are wealthier because the stock market and housing market have gone bananas.[^1] Experts also expect the Fed's report to bury data about the affordability crisis. 

**This blog will be the place to find new analyses of the Survey of Consumer Finances that explore the affordability crisis and wealth inequality**.[^2] I'll be rapidly analyzing the raw survey data and posting updated findings like these, which are drawn from my analyses of the 2022 SCF:

* The majority (52%) of working families could not afford food and housing if their income were interrupted for just one month.
* 77% of Black families could not cover food and housing if their income were interrupted for just one month.
* One in four renting households has less than $200 in cash on hand, and 42% have less than $1,000.
* I'll also highlight how much less money working families have in their checking and savings accounts now compared to 2022 - expect a number in the big billions. 

So stay tuned! 

### In the meantime...

As a build-up to late October, I'll be publishing new analyses that are newsworthy. The first are a series about World Cup ticket prices and how they compare to the last time the U.S. hosted the event in 1994. **If you are a reporter and have a question about affordability, reach out and I may be able to leverage publicly available data to answer it for you.**

### The motivating idea 

**When elites talk about "the economy," they're not talking about you.**

When the President says we have "[the greatest economy ever](https://www.euronews.com/business/2026/02/24/greatest-economy-ever-the-numbers-behind-trumps-boasting)" or the Treasury Secretary says 2026 is poised to be a "[feast and the banquet](https://www.facebook.com/watch/?v=2613868452325685)" for the American economy, they're not talking about you. They're not even talking about people.

When elites talk about "the economy," they're talking about money. More than that, they're talking about money making money.

How does money make money? By extracting it from you.

Who gets rich? The people who are already rich.

Who gets squeezed? The ~98% of us who are or have worked for pay; the 99% of us who enter the market as consumers; the 99% of us who pay subscription fees; the 99% of us whose data Facebook sells for massive profits.

### It's about *people*

**In *Our* Economy** is a blog about the economy as **people** experience it. It's about families working to survive — struggling to survive — in an economy that's rigged.

Its goal is to put **people** - not money - at the center of debates about how the economy is and isn't working.

What sets this blog apart from many other great projects about inequality is that most posts contain **new analyses of complex survey data**. I'll be repurposing economic datasets that the elite use to talk about their economy to gain insights into the economy as working people experience it. Countless sources can tell you that World Cup ticket prices are insanely expensive — this is the place where you can find hard data showing how many American families can't afford them.


### Here are the kind of pivots this blog will be making

<div class="tc-gallery" id="tc-gallery"></div>

<style>
.tc-gallery {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1.25rem;
  margin: 2rem 0;
}

@media (max-width: 500px) {
  .tc-gallery { grid-template-columns: 1fr; }
}

.tc-gallery-item {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  align-items: flex-start;
}

.tc-gallery-item > .tc-slide {
  width: 100%;
}

.tc-slide {
  aspect-ratio: 1;
  background: linear-gradient(160deg, #141820 0%, #0a0d12 100%);
  border-radius: 12px;
  box-shadow: 0 8px 28px rgba(0,0,0,0.22);
  position: relative;
  overflow: hidden;
  container-type: inline-size;
}

.tc-card-inner {
  position: absolute;
  inset: 0;
  padding: 8.5%;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.tc-accent-bar {
  position: absolute;
  top: 5.8%;
  left: 8.5%;
  width: 6.6%;
  height: 0.55%;
  min-height: 3px;
  background: #5b6af0;
  border-radius: 3px;
}

.tc-slide .tc-label-them {
  font-family: 'Inter', -apple-system, sans-serif;
  font-size: max(0.65rem, 1.35cqw);
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: #5a6380;
  margin-bottom: 0.4em;
}

.tc-slide .tc-quote {
  font-family: 'Lora', Georgia, serif;
  font-style: italic;
  font-size: max(1.05rem, 3.75cqw);
  line-height: 1.4;
  color: #e8eaf6;
  margin-bottom: 0.75em;
}

.tc-divider {
  width: 7%;
  height: 3px;
  background: #f06050;
  border-radius: 2px;
  margin-bottom: 0.75em;
  flex-shrink: 0;
}

.tc-slide .tc-label-us {
  font-family: 'Inter', -apple-system, sans-serif;
  font-size: max(0.65rem, 1.35cqw);
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: #f06050;
  margin-bottom: 0.4em;
}

.tc-slide .tc-response {
  font-family: 'Inter', -apple-system, sans-serif;
  font-size: max(0.95rem, 3.1cqw);
  line-height: 1.45;
  color: #e8eaf6;
}

.tc-card-footer {
  position: absolute;
  bottom: 5.5%;
  left: 8.5%;
  right: 8.5%;
  display: flex;
  justify-content: space-between;
  align-items: baseline;
}

.tc-slide .tc-card-url {
  font-family: 'Inter', -apple-system, sans-serif;
  font-size: max(0.6rem, 1.05cqw);
  color: rgba(90,99,128,0.85);
}

.tc-slide .tc-card-logo {
  font-family: 'Lora', Georgia, serif;
  font-size: max(0.6rem, 1.05cqw);
  color: rgba(232,234,246,0.5);
}

.tc-slide .tc-card-logo em { font-style: italic; }

.tc-dl-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.25rem;
  padding: 0.18rem 0.5rem;
  border-radius: 999px;
  border: 1px solid var(--color-border, #e5e7ef);
  background: transparent;
  cursor: pointer;
  font-family: 'Inter', -apple-system, sans-serif;
  font-size: 0.62rem;
  font-weight: 600;
  color: var(--color-text-muted, #6b7280);
  transition: border-color 150ms, color 150ms;
  align-self: flex-start;
  width: fit-content;
}

.tc-dl-btn:hover:not(:disabled) { border-color: #5b6af0; color: #5b6af0; }
.tc-dl-btn:disabled { opacity: 0.5; cursor: default; }
</style>

<script>
(function () {
  // Every string literal uses backtick template literals so Jekyll's
  // smart-quote processor never sees ' or “ as JS string delimiters.
  const SLIDES = [
    {
      economy: `Commodity prices are strong`,
      ours: `We're paying more at the pump, the grocery store, and the utility bill.`
    },
    {
      economy: `Increased productivity is driving corporate profits`,
      ours: `We're doing more work, often for the same pay, and someone else is keeping the difference.`
    },
    {
      economy: `Conditions are favorable to capital formation`,
      ours: `The rich have cut taxes on investment income and reduced regulations so that they can accumulate even more money.`
    },
    {
      economy: `”When oil prices go up, we make a lot of money”`,
      ours: `We pay at the pump while fossil fuel companies profit.`
    }
  ];

  const DL_ICON = `<svg width=”13” height=”13” viewBox=”0 0 24 24” fill=”none” stroke=”currentColor” stroke-width=”2.5”><path d=”M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4”/><polyline points=”7 10 12 15 17 10”/><line x1=”12” y1=”15” x2=”12” y2=”3”/></svg>`;

  function el(tag, cls, color) {
    const n = document.createElement(tag);
    if (cls) n.className = cls;
    if (color) n.style.color = color;
    return n;
  }

  function buildGallery() {
    const gallery = document.getElementById(`tc-gallery`);

    SLIDES.forEach((s, i) => {
      const item = el(`div`, `tc-gallery-item`);
      const slide = el(`div`, `tc-slide`);
      const inner = el(`div`, `tc-card-inner`);

      inner.appendChild(el(`div`, `tc-accent-bar`));

      const labelThem = el(`div`, `tc-label-them`, `#5a6380`);
      labelThem.textContent = `In their economy`;
      inner.appendChild(labelThem);

      const quote = el(`div`, `tc-quote`, `#e8eaf6`);
      quote.textContent = s.economy;
      inner.appendChild(quote);

      inner.appendChild(el(`div`, `tc-divider`));

      const labelUs = el(`div`, `tc-label-us`, `#f06050`);
      labelUs.appendChild(document.createTextNode(`In `));
      const ourEm = document.createElement(`em`);
      ourEm.textContent = `our`;
      labelUs.appendChild(ourEm);
      labelUs.appendChild(document.createTextNode(` economy,`));
      inner.appendChild(labelUs);

      const response = el(`div`, `tc-response`, `#e8eaf6`);
      response.textContent = s.ours;
      inner.appendChild(response);

      const footer = el(`div`, `tc-card-footer`);

      const urlSpan = el(`span`, `tc-card-url`, `rgba(90,99,128,0.85)`);
      urlSpan.textContent = `inoureconomy.org`;
      footer.appendChild(urlSpan);

      const logoSpan = el(`span`, `tc-card-logo`, `rgba(232,234,246,0.5)`);
      logoSpan.appendChild(document.createTextNode(`In `));
      const logoEm = document.createElement(`em`);
      logoEm.textContent = `Our`;
      logoSpan.appendChild(logoEm);
      logoSpan.appendChild(document.createTextNode(` Economy`));
      footer.appendChild(logoSpan);

      inner.appendChild(footer);
      slide.appendChild(inner);

      const btn = el(`button`, `tc-dl-btn`);
      btn.style.width = `fit-content`;
      btn.style.alignSelf = `flex-start`;
      btn.textContent = `Download`;
      btn.addEventListener(`click`, () => downloadSlide(s, i + 1, btn));

      item.appendChild(slide);
      item.appendChild(btn);
      gallery.appendChild(item);
    });
  }

  // ── Canvas image generation ─────────────────────────────────────────────────

  function wrapText(ctx, text, x, y, maxW, lh) {
    const words = text.split(` `);
    let line = ``, cy = y;
    for (let n = 0; n < words.length; n++) {
      const test = line + words[n] + ` `;
      if (ctx.measureText(test).width > maxW && line !== ``) {
        ctx.fillText(line.trimEnd(), x, cy);
        line = words[n] + ` `;
        cy += lh;
      } else {
        line = test;
      }
    }
    ctx.fillText(line.trimEnd(), x, cy);
    return cy + lh;
  }

  function countLines(ctx, text, maxW) {
    const words = text.split(` `);
    let line = ``, count = 0;
    for (let n = 0; n < words.length; n++) {
      const test = line + words[n] + ` `;
      if (ctx.measureText(test).width > maxW && line !== ``) { count++; line = words[n] + ` `; }
      else { line = test; }
    }
    return count + 1;
  }

  function drawLogo(ctx, rightX, y, sz) {
    ctx.font = `600 ${sz}px Lora, Georgia, serif`;
    const wIn = ctx.measureText(`In `).width;
    const wEconomy = ctx.measureText(`Economy`).width;
    ctx.font = `italic 600 ${sz}px Lora, Georgia, serif`;
    const wOur = ctx.measureText(`Our `).width;
    let x = rightX - wIn - wOur - wEconomy;
    ctx.fillStyle = `rgba(232,234,246,0.55)`;
    ctx.font = `600 ${sz}px Lora, Georgia, serif`;
    ctx.fillText(`In `, x, y); x += wIn;
    ctx.font = `italic 600 ${sz}px Lora, Georgia, serif`;
    ctx.fillText(`Our `, x, y); x += wOur;
    ctx.font = `600 ${sz}px Lora, Georgia, serif`;
    ctx.fillText(`Economy`, x, y);
  }

  function generateImage(slide) {
    const SZ = 1080, PAD = 90, W = SZ - 2 * PAD;
    const canvas = document.createElement(`canvas`);
    canvas.width = SZ; canvas.height = SZ;
    const ctx = canvas.getContext(`2d`);
    ctx.textBaseline = `top`;

    const bg = ctx.createLinearGradient(0, 0, 0, SZ);
    bg.addColorStop(0, `#141820`); bg.addColorStop(1, `#0a0d12`);
    ctx.fillStyle = bg; ctx.fillRect(0, 0, SZ, SZ);

    const LBL = 28, LBLH = LBL * 1.3;
    const QSZ = 54, QLH = QSZ * 1.45;
    const RSZ = 48, RLH = RSZ * 1.45;
    const G1 = 44, G2 = 64, G3 = 52, G4 = 44;

    ctx.font = `italic ${QSZ}px Lora, Georgia, serif`;
    const qLines = countLines(ctx, slide.economy, W);
    ctx.font = `${RSZ}px Inter, -apple-system, sans-serif`;
    const rLines = countLines(ctx, slide.ours, W);

    const contentH = LBLH + G1 + qLines * QLH + G2 + 5 + G3 + LBLH + G4 + rLines * RLH;
    let y = 100 + Math.max(0, (880 - contentH) / 2);

    ctx.fillStyle = `#5b6af0`; ctx.fillRect(PAD, 62, 72, 6);

    ctx.font = `600 ${LBL}px Inter, -apple-system, sans-serif`;
    ctx.fillStyle = `#5a6380`;
    ctx.fillText(`IN THEIR ECONOMY`, PAD, y);
    y += LBLH + G1;

    ctx.font = `italic ${QSZ}px Lora, Georgia, serif`;
    ctx.fillStyle = `#e8eaf6`;
    y = wrapText(ctx, slide.economy, PAD, y, W, QLH) + G2;

    ctx.fillStyle = `#f06050`; ctx.fillRect(PAD, y, 80, 5);
    y += 5 + G3;

    ctx.font = `600 ${LBL}px Inter, -apple-system, sans-serif`;
    ctx.fillStyle = `#f06050`;
    ctx.fillText(`IN OUR ECONOMY,`, PAD, y);
    y += LBLH + G4;

    ctx.font = `${RSZ}px Inter, -apple-system, sans-serif`;
    ctx.fillStyle = `#e8eaf6`;
    wrapText(ctx, slide.ours, PAD, y, W, RLH);

    const BOT_Y = SZ - 56;
    ctx.font = `400 23px Inter, -apple-system, sans-serif`;
    ctx.fillStyle = `#5a6380`;
    ctx.fillText(`inoureconomy.org`, PAD, BOT_Y);
    drawLogo(ctx, SZ - PAD, BOT_Y, 28);

    return canvas;
  }

  function downloadSlide(slide, n, btn) {
    const orig = btn.innerHTML;
    btn.textContent = `Generating…`;
    btn.disabled = true;
    document.fonts.ready.then(() => {
      generateImage(slide).toBlob(blob => {
        const url = URL.createObjectURL(blob);
        const a = document.createElement(`a`);
        a.href = url; a.download = `inoureconomy-${n}.png`;
        document.body.appendChild(a); a.click();
        document.body.removeChild(a); URL.revokeObjectURL(url);
        btn.innerHTML = orig; btn.disabled = false;
      }, `image/png`);
    });
  }

  buildGallery();
})();
</script>

You can dowload and share these. If you have suggestions about new/better/different permuations, send an email and I'll create them for you.

---

[^1]: Elites will say that increased housing prices are a good thing - but are they? Sky-high home prices have priced a generation (and counting) of Americans out of homeownership. And even people whose homes are more valuable on paper can't use them to buy groceries, gasoline, or health insurance.
[^2]: I am exploring other outlets as well. Reach out if you are interested in publishing or broadcasting the findings to your networks. 
